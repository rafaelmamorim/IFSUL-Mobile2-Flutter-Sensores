name: flutter_sensores
description: "Aprendendo a usar os sensores"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  battery_plus: ^6.2.3
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  sensors_plus: ^6.1.2
  sensors_plus_platform_interface: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
